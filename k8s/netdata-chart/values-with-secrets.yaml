image:
  tag: stable

parent:
  claiming:
    enabled: true
    # Token and rooms will be injected via environment variables from secret
  envFrom:
    - secretRef:
        name: netdata-claiming

child:
  claiming:
    enabled: true
    # Token and rooms will be injected via environment variables from secret
  envFrom:
    - secretRef:
        name: netdata-claiming
