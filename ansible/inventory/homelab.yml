---
# Homelab Infrastructure Inventory
# Proxmox cluster and k3s cluster

all:
  children:
    # Proxmox Infrastructure
    proxmox:
      children:
        nut_server:
          hosts:
            pve004:
        nut_netclients:
          hosts:
            pve001:
            pve002:
            pve003:
            pve005:
            pve006:
            # pve007:  # Needs BIOS VT-x/VT-d enabled
            pve008:
            pve009:
#               ansible_host: 10.150.10.49

    # Media Servers
    media_servers:
      hosts:
        jellyfin:
          ansible_user: root

    # SLURM Cluster (archived - VMs deleted 2026-01-30)
    # See ansible/archive/slurm-cluster/

    # Backup Servers
    backup_servers:
      hosts:
        pibackup:
          ansible_user: adambehn
          backup_disk_device: /dev/sda2
          backup_mount_point: /mnt/backup

    # Kubernetes Cluster (Currently decommissioned)
    # k3s_cluster:
    #   children:
    #     k3s_master:
    #       hosts:
    #         k3s-master-01:
    #     k3s_workers:
    #       hosts:
    #         k3s-worker-01:
    #         k3s-worker-02:
    #         k3s-worker-03:
    #         k3s-worker-04:
    #           ansible_host: 10.150.10.169
    #         k3s-worker-05:
    #           ansible_host: 10.150.10.170
    #         k3s-worker-06:
    #           ansible_host: 10.150.10.171
