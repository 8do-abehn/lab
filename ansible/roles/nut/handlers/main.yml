---
- name: Restart NUT services
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: yes
  loop:
    - nut-server
    - nut-monitor
  when: nut_mode == 'server'

- name: Restart NUT client services
  systemd:
    name: nut-monitor
    state: restarted
    enabled: yes
  when: nut_mode == 'client'
